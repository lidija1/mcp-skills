from ui.pages.common.base_page import BasePage


class WCQuotePage(BasePage):
    """Workers Compensation LOB quote details page.

    Locators and methods will be filled in after inspecting the UI
    via page.pause() on the first run.
    """

    def __init__(self, page):
        super().__init__(page)
        # TODO: add locators after UI inspection
